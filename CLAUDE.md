# clinic-pay — 작업 규칙

병원 정산 3-Way 대사 시스템 (Streamlit 단일 파일 `app.py`).
**이 저장소는 여러 계정에서 번갈아 개발된다.** 다음 세션·다른 계정이 맥락을 잃지 않도록
아래 인계 규칙을 반드시 지킬 것.

## 📌 작업 시작 전 (필수)

1. **`docs/DEVLOG.md` 최근 5개 항목을 읽는다** — 직전 세션들이 무엇을 왜 바꿨는지 파악.
   같은 문제를 다시 풀거나 이미 뒤집힌 결정을 되돌리는 사고를 막는다.
   (SessionStart 훅이 최근 내역을 자동으로 띄워 준다. 안 보이면 직접 열어볼 것)
2. 처음 이 저장소를 다루면 **`HANDOVER.md`를 읽는다** — 도메인·아키텍처·코드 맵·함정 전체.
3. 사용자 양식·규격 질문은 **`README.md`**가 정답.

## 📌 작업 완료 시 (필수)

**코드를 바꿨으면 반드시 `docs/DEVLOG.md` 맨 위에 항목을 추가한다.** 형식은 그 파일 상단의
템플릿을 따른다. 커밋과 같은 커밋에 포함시킬 것(별도 커밋으로 미루면 누락된다).

추가로, 다음에 해당하면 **`HANDOVER.md`도 같이 갱신**한다:

| 바뀐 것 | 갱신할 곳 |
|---|---|
| 함수 추가/삭제/이름변경, 라인 위치 크게 이동 | 4장 코드 맵 |
| 새 설계 원칙·기존 원칙 변경 | 3장 설계 원칙 |
| 새 사고·함정 발견 (실제로 터진 버그) | 7장 함정 목록 |
| 이관·배포·Secrets 관련 변경 | 1장 체크리스트 |
| 과제 완료 또는 새 과제 발견 | 8장 향후 과제 |
| 도메인 용어·채널 정의 변경 | 2장 |

> DEVLOG는 **시간순 기록**(무엇을 왜 했나), HANDOVER는 **현재 상태 스냅샷**(지금 어떻게
> 생겼나). 둘은 역할이 다르므로 한쪽만 쓰면 안 된다.

## 🔤 용어 (출력·문서·주석에서 정확히 쓸 것)

| 정식 명칭 | 코드 축약 | 실체 |
|---|---|---|
| **단말기 마감** | `한솔`/`hansol` | 한솔페이 PG 카드 승인내역 (기계가 찍은 사실) |
| **구글 일일마감** | `일마`/`daily` | 프론트 수기 구글시트 (오류 주 발생원) |
| **베가스 마감** | `차트`/`patient` | 베가스 EMR 환자별 집계 (**세무 기준원장**) |

**차트 기준 원칙**: 모든 차이는 "일마/한솔이 **차트보다** X원 많다/적다"로 서술.
부호 규약 `일-차 = +5,000` → 일마가 차트보다 5,000원 많음.

내부 변수·데이터 섹션은 축약 표기를 유지(토큰 절약)하되, **사용자에게 보이는 텍스트
(AI 출력·UI 라벨)는 정식 명칭**을 쓴다.

## ⚖️ 설계 원칙 (어기지 말 것)

1. **결정론 우선, AI는 보조** — 검증(유형B/C1/C2/C3)·채널 대사·오차요약은 전부 코드가
   확정 계산한다. AI 없이도 오류 건이 특정돼야 한다.
2. **허위 차이를 만들지 않는다** — 모호하면(승인번호가 여러 차트에 걸림·동명이인·차트번호
   결측) 매칭하지 말고 건너뛴다. 잘못 연결해 없는 오류를 만드는 것이 못 찾는 것보다 나쁘다.
3. **단정하지 않고 검토를 권고** — "차트를 50,000으로 고쳐라"가 아니라 "그 환자의 차트
   결제내역을 검토하라".
4. **import-safe 유지** — UI 코드는 반드시 `main()` 안에. 깨지면 테스트 전체가 죽는다.

## ⚠️ 자주 밟는 지뢰 (상세: `HANDOVER.md` 7장)

- 구글시트는 **`export?gid=` 1순위** — `gviz`로 되돌리면 결제수단 머리글이 사라진다.
- **조합 탐색엔 반드시 상한** — P1b 조합 폭발로 세그폴트가 났었다.
- 승인번호는 **`_appr_key`로 정규화**(선행 0 제거) — 안 하면 베가스↔한솔 매칭 전멸.
- **플랫폼 결제를 카드로 집계 금지** — 알리페이·위챗·카카오페이·큐릭·바비톡·닥터나우.
- `AI_USER` 프롬프트 수정 후 **`.format(data='x')`가 되는지 확인** (중괄호 `{{ }}` 이스케이프).
- Secrets `[clinic_daily_sheets]`는 **전체 대체** — 쓸 거면 6개 지점 전부 적을 것.

## ✅ 테스트

```bash
pip install -r requirements-dev.txt && pytest    # 141 passed 유지
```

- 실데이터에서 버그를 찾으면 **`tests/factories.py`에 합성 시나리오를 추가**하고 테스트를
  붙인 뒤 고친다. 환자 개인정보 때문에 실데이터를 커밋할 수 없어 합성 재현이 유일한 회귀
  방어선이다.
- 테스트는 streamlit 없이 돈다(`tests/conftest.py`가 경량 스텁 주입).

## 🔀 커밋·PR

- 작업 브랜치: `claude/<주제>-<suffix>` → PR로 `main`에 머지.
- 커밋 메시지는 **한국어로, "무엇을 왜"**를 적는다. 실사례가 있으면 함께
  (예: "잠실 7/15 조인태 사례").
- PR 본문은 `.github/pull_request_template.md` 양식을 채운다.
