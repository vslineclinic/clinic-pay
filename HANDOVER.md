# clinic-pay 개발 인계 문서

> **대상**: TEAM 계정에서 이 프로젝트를 이어받아 개발할 개발자
> **작성 기준**: `main` = `613ce7d` (2026-07-19) · `app.py` 5,599줄 · 테스트 141개 전부 통과
> **저장소**: `vslineclinic/clinic-pay` (GitHub) · 107 커밋 · PR 99개 (2026-03 ~ 2026-07)

이 문서 하나만 읽으면 코드를 열기 전에 **무엇을 푸는 시스템인지 · 어디에 무슨 코드가
있는지 · 어떤 함정이 있는지**를 알 수 있도록 썼다. 세부 사용법·양식 규격은 `README.md`가
정답이며, 이 문서는 **왜 그렇게 만들어졌는지(설계 의도)와 인계 시 주의점**에 집중한다.

---

## 1. TEAM 계정 이관 체크리스트

이관 직후 순서대로 확인할 것.

| # | 항목 | 확인 방법 / 주의 |
|---|---|---|
| 1 | **저장소 이전** | `vslineclinic/clinic-pay` → TEAM org로 transfer 또는 fork. **미머지 브랜치 없음** — `main`(`613ce7d`)만 가져가면 전부 |
| 2 | **Streamlit Cloud 재배포** | 앱이 Streamlit Cloud에서 돌고 있다면 TEAM 계정으로 재연결 필요. 엔트리포인트 `app.py`, Python 3.11 |
| 3 | **Secrets 이전 (★필수)** | `.streamlit/secrets.toml`은 깃에 없음. 아래 2개 섹션을 새 환경에 다시 넣어야 함 |
| 4 | **구글시트 공유 설정 유지** | 6개 지점 스프레드시트가 "링크가 있는 모든 사용자 = 뷰어"여야 읽힘. 지점이 공유를 잠그면 앱이 그 지점만 조용히 실패 |
| 5 | **Gemini API 키 발급** | https://aistudio.google.com/apikey — 무료 티어. 기존 키를 그대로 쓰면 RPD를 공유하므로 팀 전용 키 권장 |
| 6 | **차트마감 비밀번호** | 코드 기본값 `default_password="vsline99!!"` (`load_file`). 지점이 바꾸면 UI에서 직접 입력 |
| 7 | **테스트 통과 확인** | `pip install -r requirements-dev.txt && pytest` → 141 passed |

### Secrets 형식

```toml
# .streamlit/secrets.toml  (깃에 커밋하지 말 것)

GOOGLE_API_KEY = "AIza..."          # Gemini API 키 (AI 분석 탭 기본값으로 채워짐)

[clinic_daily_sheets]                # app.py의 CLINIC_DAILY_SHEETS를 덮어씀
"인천점"   = "https://docs.google.com/spreadsheets/d/.../edit"
"잠실점"   = "https://docs.google.com/spreadsheets/d/.../edit"
"강남점"   = "https://docs.google.com/spreadsheets/d/.../edit"
"일산점"   = "https://docs.google.com/spreadsheets/d/.../edit"
"압구정"   = "https://docs.google.com/spreadsheets/d/.../edit"
# 지점이 시트를 새 파일로 옮긴 경우 — [[적용시작일, URL], ...] 최신 시작일을 위에
"엔디어트" = [["2026-06-12", "https://.../새시트/edit"], ["", "https://.../옛시트/edit"]]
```

> Secrets가 없어도 앱은 뜬다 — `CLINIC_DAILY_SHEETS`의 하드코딩 URL로 폴백하고,
> API 키는 사용자가 UI에 직접 입력하면 된다. **Secrets는 편의 기능이지 필수 의존성이 아니다.**
>
> ⚠️ **함정**: `get_clinic_daily_sheets()`(L79)는 Secrets에 `[clinic_daily_sheets]`가
> **하나라도 있으면 하드코딩 목록을 통째로 버리고 Secrets만 쓴다**(부분 병합 아님).
> 한 지점만 Secrets에 적으면 나머지 5개 지점이 드롭다운에서 사라진다 —
> **Secrets를 쓸 거면 6개 지점 전부 적을 것.**

### 로컬 실행

```bash
pip install -r requirements.txt
streamlit run app.py          # http://localhost:8501

pip install -r requirements-dev.txt && pytest    # 테스트 (streamlit 없이도 돌아감)
```

`.devcontainer/devcontainer.json`이 있어 **GitHub Codespaces에서 즉시 실행** 가능
(컨테이너 attach 시 `streamlit run app.py` 자동 기동, 8501 포트 자동 포워딩).

---

## 2. 이 시스템이 푸는 문제

병원(피부·성형 클리닉) 프론트에서 **하루 마감 시 세 곳의 수납 기록이 서로 안 맞는다.**
"카드 합계가 27,400원 안 맞는데 누구 건인지 모르겠다"를 사람이 수백 건 대조로 찾던 것을,
**어느 환자의 어느 거래에서 얼마가 어긋났는지 코드로 특정**해 주는 것이 이 앱의 존재 이유다.

### 세 개의 파일 (★용어를 정확히 쓸 것)

| 정식 명칭 | 코드/축약 표기 | 실체 | 성격 |
|---|---|---|---|
| **단말기 마감** | `한솔` / `hansol` / `h` | 한솔페이(PG사) 카드 승인내역 | 기계가 찍은 **사실**. 카드만 있고 현금·플랫폼은 없음 |
| **구글 일일마감** | `일마` / `daily` / `d` | 프론트 직원이 손으로 적는 구글 스프레드시트 | **수기 = 오류의 주 발생원** |
| **베가스 마감** | `차트` / `patient` / `p` | 베가스 EMR의 환자별 수납 집계 | **세무 기준원장**. 모든 차이는 이 파일 기준으로 서술 |

> **차트가 기준(R12)** — 세무조사 시 차트 수치를 기준으로 증빙을 요구하므로, 모든 차이는
> "일마/한솔이 **차트보다** X원 많다/적다"로 표현한다. 부호 규약: `일-차 = +5,000`이면
> 일마가 차트보다 5,000원 많음.

### 3개 결제 채널

`카드` / `현금+이체` / `플랫폼`. **플랫폼**은 여신티켓·강남언니·나만의닥터·제로페이·
알리페이·위챗페이·카카오페이·간편결제(큐릭)·바비톡·닥터나우 — **카드 단말(한솔)을 거치지
않으므로 카드로 집계하면 안 된다.** 이 분류를 틀리면 채널 합계가 통째로 어긋난다
(실제로 2026-06에 큐릭/바비톡/닥터나우 오분류 버그가 있었음 → `3dd6907`).

### 두 가지 분석 모드

1. **하루 정밀 분석** — 일마+차트 2개(한솔은 선택). 하루치를 환자 단위까지 파고듦. 메인.
2. **기간 분석** — 한솔+차트 월간 파일 2개로 **어느 날에 오류가 있는지**만 빠르게 스캔.
   일일마감 불필요. 차이 있는 날만 승인번호·금액 매칭으로 미설명 거래를 0~3건으로 좁힘.

---

## 3. 아키텍처 — 데이터 흐름

`app.py` **단일 파일 모놀리스**(5,599줄). 모듈 분리를 하지 않은 것은 Streamlit Cloud
배포 단순성 때문이며, 대신 **섹션 주석(`# ━━━`)으로 12개 구간이 명확히 나뉘어 있다.**

```
[업로드/구글시트]
   한솔 xlsx        구글시트 or 파일        차트마감 xlsx(암호)
      │                    │                        │
   load_file()        load_gsheet_daily()       load_file(password)
      │                    │                        │
   parse_hansol()      parse_daily()            parse_patient()
      │                    │  └→ daily_refund(환불행 분리)
      │                    │                        │
      └──────────┬─────────┴────────────────────────┘
                 │
        cross_check_daily_patient()   ← 타 지점/날짜 파일 방어 (일치율 60% 미만이면 차단)
        filter_to_single_date()       ← 월간 파일이면 하루로 자동 축소
                 │
         run_matching()  ← 10-Pass 매칭 엔진 (626줄, 이 앱의 심장)
                 │
      ┌──────────┼──────────┬─────────────┬──────────────┐
      │          │          │             │              │
  compute_p1  compute_p2  compute_totals  build_verification  find_channel_suspects
  (한↔차)     (미매칭)    (합계)          (유형B/C1/C2/C3)     (채널별 의심후보)
      │          │          │             │              │
      └──────────┴──────────┼─────────────┴──────────────┘
                            │
                  compute_channel_recon()   ← ★메인 산출물: 채널×파일 합계·차이표
                            │
                 ┌──────────┴──────────┐
                 │                     │
          build_ai_text()        build_ai_excel()
          (AI 입력 ~100KB)       (통합 엑셀 8시트)
                 │
          run_gemini()  ← Gemini 3 Flash + 4단계 폴백 + 캐시
                 │
            AI 분석 리포트 (마크다운)
```

### 설계 원칙 (이걸 어기면 시스템이 망가진다)

1. **결정론 우선, AI는 보조.** 유형B/C1/C2/C3 검증, 채널 대사, 오차요약의 단일/조합 일치
   후보는 **전부 코드가 계산**한다. AI는 그 위에서 "왜 그런지"만 설명한다. AI 없이도 오류
   건은 100% 특정된다 — 이게 이 앱의 신뢰성 근거다.
2. **허위 차이를 만들지 않는다.** 모호하면(승인번호가 여러 차트에 걸림, 동명이인, 차트번호
   결측) **매칭하지 않고 넘어간다.** 잘못 연결해서 없는 오류를 만드는 것이 못 찾는 것보다 나쁘다.
3. **단정하지 않고 검토를 권고한다.** AI 출력은 "차트를 50,000으로 고쳐라"가 아니라
   "그 환자의 차트 결제내역을 먼저 검토하라"로 끝난다 (프롬프트 R9).
4. **import-safe.** UI 로직은 전부 `main()` 안에 있어 `import app`으로 UI가 실행되지 않는다.
   테스트가 streamlit 없이 도는 이유(`tests/conftest.py`가 경량 스텁 주입).

---

## 4. 코드 맵 — `app.py` 전 구간

라인 번호는 `5238bd1` 기준. 함수를 찾을 땐 `grep -n "^def 함수명" app.py`.

### 4.1 설정 (L29–117)

| 심볼 | 라인 | 설명 |
|---|---|---|
| `CLINIC_DAILY_SHEETS` | 65 | 지점→구글시트 URL. **Secrets가 있으면 덮어씀** |
| `CROSS_CHECK_MIN_RATE` | 53 | 교차검증 임계값 0.6. 이 값 미만 일치율이면 분석 차단 |
| `DAILY_SHEET_DATE_FMT` | 47 | 시트 탭 날짜 형식 기본값 `%y.%m.%d` |
| `resolve_daily_sheet_url()` | 98 | 지점이 시트를 새 파일로 옮긴 경우 날짜별로 URL 선택 (엔디어트) |

### 4.2 유틸리티 (L120–425)

| 함수 | 라인 | 핵심 |
|---|---|---|
| `clean_no` | 134 | 숫자만 추출. **NFKC 정규화** — 전각숫자(１５６０)를 반각으로. 차트 결제메모에 전각 승인번호가 들어와 매칭이 조용히 실패하던 버그 대응 |
| `_appr_key` | 143 | 승인번호 매칭 키 = `clean_no` + **선행 0 제거**. 베가스는 8자리 0패딩(`00873971`), 한솔은 자연수(`873971`) → 이거 없으면 매칭 전멸 (`c933f87`) |
| `_parse_clock` | 165 | `HH:MM:SS` / `HHMMSS` / `YYYY-MM-DD HH:MM:SS` 혼용 처리. 구 버전 `zfill(6)` 방식이 날짜 붙은 값에서 시각을 오독하던 버그 수정 |
| `load_file` | 326 | **파일 읽기 최종 방어선**. xlsx/xls/xlsb/csv/HTML위장 전부 시도 + msoffcrypto로 암호 해제. 확장자를 믿지 않고 내용으로 판별 |

### 4.3 구글시트 연동 (L428–715)

가장 까다로웠던 영역. 구글의 CSV 내보내기 방식 2종을 우선순위로 쓴다.

- **1순위 `export?format=csv&gid=`** — 셀 원본 그대로. 결제수단 머리글(카드/현금/이체)이
  보존된다. 단 탭을 **gid(숫자)**로만 지정 가능 → `_sheet_gid_map`이 edit 페이지 HTML에서
  `{탭이름: gid}`를 정규식으로 1회 긁어 세션 캐싱.
- **2순위 `gviz` CSV 폴백** — gid 매핑 실패 시. **함정**: gviz는 탭을 못 찾으면 오류 대신
  **첫 시트로 조용히 폴백**한다 → 존재할 수 없는 sentinel 이름의 응답과 비교해 폴백을
  탐지하고 "해당 날짜 탭 없음"으로 처리 (엉뚱한 날짜 데이터를 진짜로 오인하는 사고 방지).
- `_candidate_tab_names` (584) — 지점마다 탭 이름 형식이 다름(`26.06.02`/`06.02`/`6.2`/
  `2026.06.02`/`26년 06월 02일`). 후보를 우선순위대로 생성해 순차 시도. **새 형식이 나오면
  이 함수에 후보만 추가하면 된다.**

> ⚠️ gviz를 1순위로 되돌리지 말 것. gviz는 '숫자 열'의 텍스트 머리글을 타입 추론 과정에서
> 빈칸으로 떨어뜨려, 일산·강남 등에서 결제수단 머리글이 통째로 사라지고 금액이 0원으로 읽혔다.

### 4.4 파서 (L718–1378)

| 함수 | 라인 | 핵심 |
|---|---|---|
| `parse_hansol` | 723 | 헤더 자동탐지. **조회성 거래(포인트조회·잔액조회·취소조회)를 합계에서 제외** — 이걸 포함하면 합계가 부풀려짐 (`0828c2a`). `tx_status`(정상/취소/거절), `is_현금`(현금영수증) 부여 |
| `parse_daily` | 964 | 162줄. 동적 헤더 탐지 + 결제수단 컬럼 매핑(`_PAY_ALIAS`) + **환불/취소 행 분리**(`daily_refund`로 별도 반환) |
| `_relabel_pay_columns` | 910 | **비표준 지점 양식 자동복구**: 머리글이 비었거나 어긋난 금액열을, 시트 하단 요약블록 합계와 **정확히 대조**해 표준 채널명으로 재라벨링 |
| `_looks_like_chart_no` | 814 | 머리글 없는 차트번호 열 자동인식 (값의 70%+가 3~7자리 정수) |
| `parse_patient` | 1208 | 171줄. 결제수단 정밀분류. **`간편결제(현금영수증)`을 '현금영수증' 글자에 속지 않고 플랫폼으로 분류**하는 등 예외 다수 |
| `cross_check_daily_patient` | 1152 | 타 지점/날짜 파일 방어. `block`/`warn`/`ok` 3종 판정 |

### 4.5 매칭 엔진 `run_matching` (L1428–2053, 626줄)

**이 앱에서 가장 복잡하고 가장 건드리기 위험한 함수.** 한솔 거래 ↔ 차트 수납을 14개 패스
(+ P6a/b, P9a~d 하위 단계)로 연결한다. 앞 패스일수록 확신도가 높고, 뒤 패스는 앞에서
축적된 정보(카드번호·카드사 레퍼런스)를 재활용한다.

| 패스 | 라인 | 방법 |
|---|---|---|
| P1 | 1500 | 승인번호 직접매칭 (가장 확실) |
| P1b | 1512 | 공동결제 합산 — 1장의 카드로 N명 결제. **여기서 조합 폭발로 세그폴트가 났었다** (`04b77f9`) |
| P1c | 1564 | 공유카드 — 동일 카드번호를 2인 이상이 사용 |
| P2 / P2b | 1606/1616 | 유일 금액 1:1 / 동일금액 다건은 카드사로 구분 |
| P3 / P3b | 1636/1652 | 분할결제 2~3건 합(시간 근접) / 차트 본부금 힌트 활용 |
| P4 | 1684 | 시간-순서 상관 (동일금액 다건, 카드사 우선) |
| P5 / P5b | 1725/1749 | 현금영수증 / 복합결제(1인이 카드+현금+이체) |
| P6 (a,b) | 1786 | 한솔↔일마 매칭 결과로 차트 레퍼런스를 만들어 재매칭 |
| P7 / P8 | 1828/1907 | 분할결제 크로스레퍼런스 / 차트 승인번호 힌트 보강 |
| P9 (a~d) | 1945 | 누락건 소급재검토 — 모든 패스 후 축적 정보로 재시도 |

> ⚠️ **조합 탐색은 반드시 유계로.** `_find_split_combo`(1386)는 후보 수·조합 크기(rs)·시간
> 윈도우로 제한한다. P1b에서 이 제한이 느슨해 조합이 폭발 → Streamlit 세그폴트가 발생했다.
> 새 조합 로직을 추가할 땐 최악의 경우 반복 횟수를 반드시 계산해 볼 것.

### 4.6 집계·대사 (L2056–2205)

| 함수 | 라인 | 산출 |
|---|---|---|
| `compute_p1` | 2061 | 차트번호별 [차트 카드금액 vs 한솔 매칭 카드합] |
| `compute_p2` | 2121 | 양쪽 미매칭 건 (PG-only / front-only) |
| `compute_totals` | 2133 | 파일별 채널 합계. **환불은 카테고리별 net** |
| `compute_channel_recon` | 2171 | ★메인 산출물. 채널×파일 합계 + 한-차/한-일/일-차 차이 |

### 4.7 기간(다일) 대사 (L2208–2494)

`compute_period_recon`(2232)가 일자별 한솔↔차트 대사표를 만들고, 차이 있는 날만
`find_period_day_detail`(2264)이 3단계(승인번호 → 정상/취소 상쇄 → 금액 유일매칭)로 좁힌다.
`pair_period_typo_suspects`(2347)는 남은 잔여 건에서 **금액 오타 쌍**(자릿수 추가/누락,
한 자리 오타, 자리 뒤바뀜 — `_amount_typo_kind`)을 자동 페어링한다.
`augment_period_with_gsheet`(2416)로 구글시트 일일마감 합계까지 붙일 수 있다.

### 4.8 채널 의심후보 (L2497–2967)

| 함수 | 라인 | 역할 |
|---|---|---|
| `_chart_method_pivots` | 2506 | **모든 환자단위 비교의 공통 기반.** 차트번호별 결제수단 pivot을 차트·일마 양쪽으로. 일마 환불 net 차감, 성명→차트번호 보강 링크, 유형B 병합까지 여기서 |
| `_merge_typeB_pairs_into_pivots` | 2572 | 일마의 차트번호 오타를 차트마감 번호로 귀속 → 금액이 맞는 단순 오타가 차이 목록을 오염시키지 않음 |
| `_hansol_card_by_chart` | 2642 | 승인번호를 다리로 한솔 카드금액을 차트번호에 귀속. **승인번호가 정확히 한 차트에만 대응할 때만** (공동결제 제외 = 허위 차이 방지) |
| `_build_name_map` | 2619 | 차트번호→이름. suspects/3way/AI 세 곳이 공유 (2026-07 중복 통합) |
| `find_channel_suspects` | 2690 | 278줄. multiset diff + 승인번호 cross-match로 후보 추출. 등급: `★★ 승인번호확정` > `★ 금액·gap매칭` > 일반 |

### 4.9 AI 분석 (L2970–3818)

**2026-07에 가장 많이 바뀐 영역.** 구조:

```
build_gap_summary_lines()  → [오차요약] 섹션 생성 (코드 확정계산)
build_ai_text()            → 9개 섹션 조립, 최대 100KB
AI_SYSTEM (L3589)          → 규칙 R0~R16 (프롬프트 시스템 지시)
AI_USER   (L3679)          → STEP0~7 분석 절차 + 출력 형식 템플릿
run_gemini()               → 호출 + 폴백 + 캐시
```

**`build_ai_text` 섹션 구성** (차이 0이면 META+채널대사만 반환하고 조기 종료):

| 섹션 | 내용 | 캡 |
|---|---|---|
| `[META]` | 파일별 건수·총액 | – |
| `[채널대사]` | 채널×파일 합계·차이 | – |
| `[오차요약]` | **차트 기준 확정 서술 + 단일/조합 일치 후보 + 검산** | – |
| `[데이터검증]` | 유형B/C1/C2/C3 확정건 | 유형별 50 |
| `[★★승인번호확정]` | 한솔↔차트 승인번호 매칭 불일치 | 30 |
| `[차트번호별 3-way 통합]` | 환자 1명 = 1줄 (차트/일마/한솔 채널별) | 200 |
| `[원시거래행]` | 차이 큰 환자의 개별 행 (승인번호·시각 포함) | 40명 |
| `[한솔 PG-only]` | PG에만 있는 카드건 + 동일금액·오타패턴 hint | 100 |
| `[일마 front-only]` | 일마에만 있는 카드건 | 100 |
| `[차트환불/일마환불]` | 음수 행 | 각 40 |

**`build_gap_summary_lines`(3022)가 하는 일** — AI가 부호·금액을 창작하지 못하도록 코드가
먼저 확정한다:
1. "구글 일일마감이 베가스 마감보다 X원 많음/적음" 문장 생성
2. **단일일치후보** — 환자 1명의 차이가 채널 차이와 1원 단위로 정확히 일치하면 지목
3. **조합일치후보** — 없으면 2~3건 조합 탐색 (`_find_exact_combos`, 상위 20건으로 유계)
4. **검산** — 환자별 차이 합 vs 채널 차이 → 미특정 잔여 명시
5. **오류 위치 힌트** — 한-일 차이와 일치하는 PG-only/front-only 건 지목

**프롬프트 규칙 R0~R16** (`AI_SYSTEM`) 요약:

| 규칙 | 내용 |
|---|---|
| R0 | `[데이터검증]`은 코드 확정 사실 — 최우선 신뢰, 전건 보고 |
| R1 | 입력에 실제 있는 값만 인용 (창작·반올림 금지) |
| R2 | 상쇄되어 합계에 안 보이는 환자쌍도 각각 나열 |
| R3/R4 | 우선순위 ★★ > ★ > 일반, 동급은 금액 큰 순 |
| R5 | 부호 규약 (`한-차=+10,000` = 한솔이 차트보다 많음) |
| R6/R6b | 한솔=일마≠차트면 차트 의심 / 한솔 없으면 단정 금지 |
| R7/R8 | 결제수단 오기재 패턴 |
| R9 | "확인 바랍니다" 식 일반론 금지 — 환자·채널·금액 특정 |
| R10 | 채널차이 = Σ건별차이 검산, 잔여 명시 |
| R11 | 금액 오타 패턴(×10, 한자리, 자리바뀜)은 한 쌍으로 |
| R12 | **차트 기준 원칙** + 유형B는 안내만 |
| R13 | 위치 태그 명시 `[차트마감에만 존재]` 등 |
| R14 | 환불 표기 방식 차이(이중차감/미반영) |
| R15 | 원시거래행을 행 단위로 재계산 |
| R16 | **단일/복합 원인 분기** — 단일 확실하면 1건만, 복합이면 TOP3~5 |

**Gemini 호출**(`run_gemini` 4529):
- 폴백 순서: `gemini-3-flash` → `2.5-flash-lite` → `2.5-flash` → `2.0-flash` → `2.0-flash-lite`
- 모델 ID 개편 대비 별칭(`gemini-3-flash-preview`, `gemini-flash-latest`)까지 시도
- **병목은 RPD(일일 호출수)이지 토큰이 아님** — 그래서 입력을 100KB까지 크게 보낸다
- `thinking` 활성: 3.x는 기본, 2.5는 `thinking_budget=4096`, **2.0은 넣으면 400 에러**
- 캐시: 세션 + 파일(`tempfile.gettempdir()/clinic_pay_ai_cache`), 키는 데이터+질문 md5

### 4.10 데이터 검증 `build_verification` (L3963–4426)

**AI 없이 코드만으로 오류 건을 확정하는 모듈.** 어떤 AI를 쓰든 결과가 동일하다.

| 유형 | 판정 | 처리 |
|---|---|---|
| **B 차트번호오타** | 이름·금액 동일 + 번호만 상이 | 베가스는 번호 자동기재라 오류는 **항상 일마 쪽** → 정정 안내만, 차이금액 집계 제외 |
| **C1 한쪽만존재** | 한 파일에만 있는 환자 | 위치 명시. `동일금액상대`가 있으면 한 쌍으로 |
| **C2 금액불일치** | 동일 번호·금액 상이 | 환불 이중차감 자동 진단 + **한솔 3자 판정** |
| **C3 결제수단불일치** | 총액 동일·채널 분배 상이 | **총액 비교로는 안 보이는 채널 차이의 직접 원인** |

**환불 이중차감 진단**(`_verif_refund_cause` 4183) — 베가스는 환불을 `취소(음수)+재승인(양수)`,
일마는 `본행+환불행 차감`으로 적는다. 일마 본행이 이미 환불 반영된 재승인액인데 환불 행을
또 빼면 이중차감. 차이가 환불 행 금액과 정확히 일치하면 자동 진단
(실사례: 잠실 26.07.15). `_verif_hansol_verdict`(4207)가 한솔 카드 net으로 **어느 파일이
틀렸는지** 3자 판정까지 한다.

### 4.11 UI (L4601–5595)

`main()`(4944, 652줄)이 전부. 하루 분석 결과는 탭 4개로 나뉜다 (L5304):

1. **🎯 차이 원인 추적 (★메인)** — 채널 합계 대사표 + 채널별 의심후보
2. **🔬 1:1 매칭 상세 (보조)** — 차트번호별 카드 / 한솔 미매칭 / 일마 미매칭 (하위 탭 3개)
3. **🤖 AI 진단** — API 키·모델 선택, 전송 데이터 미리보기, 분석 실행, 통합 엑셀 다운로드
4. **🔎 데이터 검증 (오타·누락·불일치)** — 유형B/C1/C2/C3 표 + 검증 엑셀

`_render_period_results`(4676)·`_period_mode_ui`(4854)는 기간 분석 모드 전용.
세션 상태 키: `done`(하루 분석 완료), `period_done`(기간 분석 완료), `_ai_result`,
`_ai_data`, `_api_key`, `_model_choice` 등. **`build_verification`은 tab3·tab4가 공유하므로
분석당 1회만 계산**(L5301)한다.

---

## 5. 개발 히스토리

106 커밋을 시기별로 묶었다. **"왜 바뀌었는지"**가 핵심이므로 각 시기의 문제의식을 적었다.

### 1기 (2026-03~04): 기초 + AI 도입 — 31 커밋

파일 파싱과 매칭 엔진의 뼈대. 후반은 **거의 전부 Gemini 무료 한도와의 싸움**이었다.

- `0828c2a` 한솔 **조회성 거래**(포인트조회 등)를 합계에서 제외 — 합계가 부풀려지던 버그
- `2020311` AI 자동 분석 도입 (Claude + Gemini 병행)
- `a7eeca0`→`18fb017` 429 한도 초과 대응 반복: 재시도+백오프 → 슬라이딩 윈도우 rate
  limiter → 토큰 절약 → 팝업 UI. **이 시기 교훈: 무료 티어 병목은 RPD이지 토큰이 아니다**
  (나중에 이 인식이 뒤집혀 입력을 오히려 늘리는 방향으로 재설계됨)
- `fc392a9`/`e39cc97` `st.dialog` 팝업 → 인라인 폼 (Streamlit 버전 이슈)

### 2기 (2026-05): 채널 합계 대사로 재설계 — 27 커밋

**가장 중요한 방향 전환.** "1:1 매칭으로 모든 거래를 연결한다"에서 **"채널 합계 차이를 먼저
내고, 그 차이를 설명할 후보만 추적한다"**로 목표를 바꿨다. 1:1 매칭은 후보 식별 도구로 강등.

- `fd6e9b7` **채널 합계 대사 중심 재설계** + Gemini 폴백 — 현재 구조의 출발점
- `dd70ecd` 의심후보 정렬을 **채널 차이값 근접도** 기준으로 (`_rank_key`)
- `dec944c` **★★ 동일환자 확정** — 승인번호 cross-match. 등급 체계의 시작
- `354dac1` `google-generativeai` → `google-genai` 패키지 마이그레이션
- `1342700` **일마+차트 2파일만으로도 분석 가능**하게 (한솔 선택화)
- `87ab891` AI용 **3-way 통합 데이터 구조** + 정확도 중심 프롬프트 재설계
- `6d9dec6`/`f1ee798` 실데이터 검증 기반 정확도 보강 — pivot 단일화, 승인번호 합계비교,
  합계행 오인 방지, 한솔 차트귀속

### 3기 (2026-06): 구글시트 연동 + 검증 모듈 — 39 커밋 (최다)

지점 실데이터를 붙이면서 **양식 비표준화와의 전쟁**이 시작됐다.

- `ef4f6de` **테스트 스위트 추가 + app.py import-safe 리팩터링** — 회귀 방어선 구축
- `49f2745` 시간 파싱 버그 + 분할매칭 **O(n³)→O(n²)** 가속
- `82eaf13`→`775423f` 구글시트 연동: 지점 드롭다운 + 달력 → 교차검증 → **무라벨 차트번호
  열 자동복구**
- `b78da44` **비표준 양식 자동복구** — 하단 요약블록↔금액열 합계 대조로 결제수단 재라벨링
- `444200f` 지점 추가(일산·압구정) + **export 경로로 전환**(gviz가 머리글을 날리는 문제)
- `355a93a`/`3dd6907` 알리페이·위챗·카카오페이 / 큐릭·바비톡·닥터나우 **플랫폼 집계 추가**
  — 카드로 잘못 잡히던 결제수단들
- `001f5da`/`2113ca1` **데이터 검증 탭 신설**(유형B/C1/C2) + AI 진단에 통합
- `f499512` **기간(월간) 대사 모드** 추가 + 환불 분류 버그 3건
- `05ad298` **차트 기준 원칙 확립**(R12) — 유형B는 안내만, 차이 위치 명시, 일마 환불 net 비교
- `c933f87` **승인번호 0패딩 매칭 실패 수정** — 베가스 `00873971` vs 한솔 `873971`

### 4기 (2026-07): 안정성 + AI 추론 강화 — 8 커밋

- `04b77f9` **P1b 조합 폭발로 인한 세그폴트 수정** — 프로덕션 크래시
- `91935f3` **환불 이중차감 자동 진단 + 한솔 3자 판정** (잠실 7/15 실사례)
- `95a225b` AI 추론 강화: **thinking 활성 + 입력 데이터 대폭 확대 + 원시거래행 섹션**
  — "RPD가 병목이니 입력을 크게 보내도 된다"는 인식 전환의 결과
- `6012a5d` '이번 오차의 원인 TOP3~5' 별점 중요도 출력
- `9e6ed73` **오차 요약 우선 + 단일/복합 원인 분기** — TOP3 강제 채우기 제거.
  "먼저 얼마를 찾아야 하는지 알려달라"는 현장 요구 반영
- `5238bd1` **용어 정식 명칭 통일** (단말기 마감 / 구글 일일마감 / 베가스 마감)

### 현재 브랜치 상태

**미머지 작업 없음.** PR #99까지 전부 `main`에 머지 완료 (`main` = `613ce7d`).
작업 브랜치는 `claude/*` 네이밍으로 만들고 PR로 `main`에 머지하는 흐름을 유지해 왔다
(총 99개 PR). 이관 시 `main` 하나만 가져가면 되고 정리할 미완 브랜치는 없다.

---

## 6. 테스트

```bash
pip install -r requirements-dev.txt && pytest     # 141 passed
```

| 파일 | 개수 | 커버리지 |
|---|---|---|
| `test_gsheet.py` | 38 | 구글시트 URL·gid 매핑·탭 이름 후보·gviz 폴백 탐지 |
| `test_period.py` | 23 | 기간 대사·일자별 차이·오타 쌍 페어링 |
| `test_parsers.py` | 22 | 한솔/일마/차트 파싱·결제수단 분류·환불 분리 |
| `test_utils.py` | 19 | 시간 파싱·금액 정리·승인번호 키(파라미터화 다수) |
| `test_verification.py` | 14 | 유형B/C1/C2/C3·환불 진단·한솔 판정 |
| `test_outputs.py` | 11 | AI 텍스트 섹션·오차요약·조합탐색·엑셀 |
| `test_reconciliation.py` | 8 | 합계·채널 대사·환불 net |
| `test_matching.py` | 6 | 승인번호 매칭·분할결제 |

**`tests/conftest.py`가 streamlit 경량 스텁을 주입**하므로 streamlit 설치 없이도 순수 로직이
테스트된다. `tests/factories.py`에 합성 데이터 빌더(`basic_three_files`,
`mismatch_three_files`)가 있어 새 시나리오를 쉽게 만들 수 있다.

> **회귀 방어 원칙**: 실데이터에서 버그를 찾으면 **반드시 그 케이스를 factories에 합성
> 시나리오로 추가**하고 테스트를 붙인 뒤 고칠 것. 이 프로젝트는 실데이터를 커밋할 수 없으므로
> (환자 개인정보) 합성 재현이 유일한 회귀 방어선이다.

---

## 7. 함정 목록 — 신규 개발자가 밟기 쉬운 지뢰

1. **`gviz`를 1순위로 되돌리지 말 것** — 결제수단 머리글이 사라진다. `export?gid=`가 1순위.
2. **조합 탐색에 상한을 반드시 걸 것** — P1b 조합 폭발로 세그폴트가 났다(`04b77f9`).
   `_find_exact_combos`도 상위 20건으로 유계.
3. **승인번호는 `_appr_key`로 정규화** — 선행 0을 제거하지 않으면 베가스↔한솔 매칭이 전멸.
4. **전각숫자** — `clean_no`가 NFKC 정규화를 한다. 우회하지 말 것.
5. **플랫폼 결제를 카드로 집계하지 말 것** — 알리페이·위챗·카카오페이·큐릭·바비톡·닥터나우는
   한솔을 거치지 않는다. `_PLATFORM_CHANNELS` 확인.
6. **`간편결제(현금영수증)`** — '현금영수증' 글자가 있어도 **플랫폼**이다.
7. **모호하면 매칭하지 말 것** — 승인번호가 여러 차트에 걸리거나 동명이인이면 건너뛴다.
   허위 차이를 만드는 것이 못 찾는 것보다 나쁘다.
8. **환불은 net으로** — 차트는 음수 행으로, 일마는 별도 `daily_refund`로 관리된다.
   한쪽만 net 처리하면 당일 결제·당일 환불 환자가 허위 차이로 잡힌다.
9. **`AI_USER`의 중괄호** — `.format(data=...)` 를 쓰므로 템플릿 자리표시자는 `{{ }}`로
   이스케이프해야 한다. 프롬프트 수정 후엔 `AI_USER.format(data='x')`가 되는지 확인할 것.
10. **`gemini-2.0`에 `thinking_config`를 넣으면 400 에러** — `_gemini_gen_config` 분기 유지.
11. **`main()` 밖에 UI 코드를 쓰지 말 것** — import-safe가 깨지면 테스트가 전부 죽는다.
12. **차트마감은 EMR 자동기재 = 차트번호 오류 불가** — 번호 불일치는 **항상 일마 쪽 오류**로
    판정한다(R12). 이 전제를 뒤집으면 유형B 처리가 통째로 잘못된다.

---

## 8. 향후 과제 (우선순위 순)

| # | 과제 | 이유 |
|---|---|---|
| 1 | **`main()` 652줄 분해** | UI 탭별 함수로 쪼개면 테스트 가능해짐. 현재 UI는 회귀 테스트가 없다 |
| 2 | **`run_matching` 626줄 패스별 분리** | 15개 패스가 한 함수에 있어 단위 테스트가 어렵다. 패스별 함수 + 순차 실행기 구조로 |
| 3 | **`app.py` 모듈 분리** | 5,599줄 단일 파일. `parsers/`·`matching/`·`ai/`·`ui/`로 나누되 Streamlit Cloud 배포 경로 확인 필요 |
| 4 | **지점 시트 설정을 코드 밖으로** | `CLINIC_DAILY_SHEETS` 하드코딩 URL을 전부 Secrets로 이전 (지점 추가 시 배포 불필요) |
| 5 | **AI 응답 구조화 출력** | 현재 마크다운 자유 서술 → JSON 스키마로 받으면 UI에서 표·필터링 가능 |
| 6 | **UI 회귀 테스트** | Streamlit AppTest(`streamlit.testing.v1`)로 탭 렌더링 스모크 테스트 |
| 7 | **캐시 TTL** | `_cache_set`에 ts는 적히지만 만료 로직이 없다. 임시디렉터리라 재시작 시 사라지지만 명시적 TTL 권장 |

---

## 9. 참고 자료

- `README.md` — 사용자 관점 매뉴얼 (일일마감 표준 양식 규격이 여기 있다 ★)
- `templates/일일마감_표준양식.xlsx` — 지점 배포용 템플릿
- `.devcontainer/devcontainer.json` — Codespaces 즉시 실행 설정
- 코드 내 섹션 주석(`# ━━━`)이 12개 구간의 목차 역할을 한다
